# udev rules for picostream USB device
# Copy this file to /etc/udev/rules.d/ and reload rules:
#   sudo cp 99-picostream.rules /etc/udev/rules.d/
#   sudo udevadm control --reload-rules && sudo udevadm trigger

SUBSYSTEM=="usb", ATTR{idVendor}=="1209", ATTR{idProduct}=="0001", MODE="0666", TAG+="uaccess"
